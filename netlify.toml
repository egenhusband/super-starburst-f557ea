[build]
  # 현재 위치(root)를 배포 폴더로 지정합니다.
  publish = "."
  # 서버리스 함수 위치를 지정합니다.
  functions = "netlify/functions"

[[redirects]]
  # 새로고침 시 404 에러 방지 설정입니다.
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  # 에러가 났던 부분을 최신 규격에 맞게 수정했습니다.
  node_bundle = "esbuild"
